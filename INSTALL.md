# 📦 Инструкции по установке PromtSpace

## Требования

Перед началом работы необходимо установить:
- **Node.js** версии 18.0.0 или выше
- **npm** (поставляется вместе с Node.js)

## 🔧 Установка Node.js на macOS

### Вариант 1: Через Homebrew (Рекомендуется)

1. **Установите Homebrew** (если еще не установлен):
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

2. **Установите Node.js через Homebrew**:
```bash
brew install node
```

3. **Проверьте установку**:
```bash
node --version  # должна быть версия >= 18.0.0
npm --version   # должна быть версия >= 9.0.0
```

### Вариант 2: Через официальный установщик

1. Скачайте установщик с официального сайта: https://nodejs.org/
2. Выберите LTS версию (Long Term Support)
3. Запустите скачанный `.pkg` файл
4. Следуйте инструкциям установщика
5. После установки перезапустите терминал

### Вариант 3: Через NVM (Node Version Manager)

1. **Установите NVM**:
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
```

2. **Перезапустите терминал** или выполните:
```bash
source ~/.zshrc
```

3. **Установите Node.js**:
```bash
nvm install 18
nvm use 18
```

4. **Проверьте установку**:
```bash
node --version
npm --version
```

## 🚀 Установка и запуск PromtSpace

После установки Node.js выполните следующие шаги:

### 1. Перейдите в директорию проекта

```bash
cd /Users/antonbolyatko/Documents/PromtSpace
```

### 2. Установите зависимости

```bash
npm install
```

Будут установлены следующие пакеты:
- `react` - библиотека для создания UI
- `react-dom` - рендеринг React в DOM
- `lucide-react` - библиотека иконок
- `express` - веб-сервер для backend
- `vite` - сборщик и dev-сервер
- `typescript` - поддержка TypeScript
- `@vitejs/plugin-react` - плагин Vite для React

Установка займет 1-2 минуты.

### 3. Запустите проект в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

Браузер должен открыться автоматически. Если этого не произошло, откройте браузер вручную и перейдите по указанному адресу.

### 4. Сборка для production (опционально)

Для создания оптимизированной production версии:

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### 5. Запуск production сервера

```bash
npm start
```

Это выполнит сборку и запустит Express сервер на порту 3000.
Приложение будет доступно по адресу: **http://localhost:3000**

## 📝 Полезные команды

```bash
# Разработка с hot-reload
npm run dev

# Сборка для production
npm run build

# Предпросмотр production build
npm run preview

# Запуск только Express сервера (требует предварительной сборки)
npm run server

# Полный production запуск (build + server)
npm start
```

## 🐛 Решение проблем

### Ошибка "command not found: npm"

**Решение:** Node.js не установлен или не добавлен в PATH. Следуйте инструкциям по установке Node.js выше.

### Ошибка "EACCES: permission denied"

**Решение:** Проблемы с правами доступа к глобальным пакетам npm.

```bash
# Для macOS/Linux - измените владельца папки npm
sudo chown -R $USER /usr/local/lib/node_modules
```

### Ошибка "Port 5173 is already in use"

**Решение:** Порт уже занят другим процессом.

```bash
# Найдите процесс на порту 5173
lsof -ti:5173

# Остановите процесс (замените PID на номер из предыдущей команды)
kill -9 PID
```

Или просто измените порт в `vite.config.ts`:

```typescript
export default defineConfig({
  server: {
    port: 3001  // Используйте другой порт
  }
})
```

### Ошибка при сборке TypeScript

**Решение:** Очистите кэш и переустановите зависимости:

```bash
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с backdrop-filter (эффект blur не работает)

**Решение:** Это CSS свойство требует современный браузер.

Поддерживаемые браузеры:
- Chrome/Edge 76+
- Safari 9+
- Firefox 103+

Обновите браузер до последней версии.

## 📚 Дополнительные ресурсы

- [Node.js документация](https://nodejs.org/docs/)
- [npm документация](https://docs.npmjs.com/)
- [Vite документация](https://vitejs.dev/)
- [React документация](https://react.dev/)
- [Tailwind CSS документация](https://tailwindcss.com/)

## ✅ Проверочный список

После установки убедитесь что:

- [ ] Node.js версии >= 18.0.0 установлен
- [ ] npm версии >= 9.0.0 установлен
- [ ] Все зависимости установлены (`node_modules/` существует)
- [ ] `npm run dev` запускается без ошибок
- [ ] Приложение открывается в браузере на localhost:5173
- [ ] Видны 3 категории: Маркетинг, Разработка, Креатив
- [ ] Поиск работает корректно
- [ ] Glass эффекты видны (полупрозрачные карточки с blur)
- [ ] Модальное окно открывается при клике на промт
- [ ] Копирование промтов работает

## 🎉 Готово!

Если все шаги выполнены успешно, ваш портал PromtSpace готов к использованию!

При возникновении проблем проверьте консоль браузера (F12) и терминал на наличие ошибок.

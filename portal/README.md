# 💎 Prompt Portal

Портал для хранения и управления промтами с дизайном в стиле Apple Liquid Glass.

![Prompt Portal Preview](https://via.placeholder.com/800x400/f5f5f7/007AFF?text=Prompt+Portal)

## ✨ Особенности

- 🎨 **Apple Liquid Glass дизайн** — современный glassmorphism с полупрозрачными элементами и размытием
- 📱 **Адаптивность** — отлично работает на десктопе, планшете и мобильных устройствах
- 🔍 **Живой поиск** — мгновенный поиск по всем промтам
- 📋 **Быстрое копирование** — копирование промта в один клик
- 🚀 **Простое развёртывание** — не требует базы данных, все данные в JSON файлах
- 🇷🇺 **На русском языке** — весь интерфейс на русском

## 🛠 Технологии

- **Фронтенд:** Чистый HTML, CSS, JavaScript (без фреймворков)
- **Бэкенд:** Node.js + Express
- **Хранение данных:** JSON файлы

## 📦 Установка

### Требования

- [Node.js](https://nodejs.org/) версии 14 или выше
- npm (устанавливается вместе с Node.js)

### Шаги установки

1. **Скачайте или клонируйте проект:**
   ```bash
   # Если используете git
   git clone <url-репозитория>
   cd portal
   
   # Или просто скопируйте папку portal в нужное место
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Запустите сервер:**
   ```bash
   npm start
   ```

4. **Откройте в браузере:**
   ```
   http://localhost:3000
   ```

## 📁 Структура проекта

```
/portal
├── public/                    # Статические файлы (фронтенд)
│   ├── css/
│   │   └── styles.css        # Все стили приложения
│   ├── js/
│   │   └── app.js            # JavaScript логика
│   ├── images/               # Изображения (если нужны)
│   └── index.html            # Главная страница
├── data/                      # Данные (JSON файлы)
│   ├── categories.json       # Список категорий
│   ├── categories/           # Промты по категориям
│   │   ├── marketing.json
│   │   ├── coding.json
│   │   └── writing.json
│   └── prompts/              # Полные тексты промтов
│       ├── marketing-1.json
│       ├── marketing-2.json
│       └── ...
├── server.js                  # Сервер Express
├── package.json              # Зависимости проекта
└── README.md                 # Этот файл
```

## ➕ Добавление новых промтов

### 1. Добавление в существующую категорию

**Шаг 1:** Создайте файл промта в `/data/prompts/`:

```json
{
  "id": "marketing-6",
  "categoryId": "marketing",
  "title": "Название промта",
  "description": "Краткое описание промта",
  "prompt": "Полный текст промта..."
}
```

**Шаг 2:** Добавьте промт в файл категории `/data/categories/marketing.json`:

```json
{
  "categoryId": "marketing",
  "prompts": [
    // ... существующие промты ...
    {
      "id": "marketing-6",
      "title": "Название промта",
      "description": "Краткое описание промта"
    }
  ]
}
```

**Шаг 3:** Обновите счётчик в `/data/categories.json`:

```json
{
  "id": "marketing",
  "name": "Маркетинг",
  "image": "📈",
  "promptCount": 6  // Было 5, стало 6
}
```

### 2. Добавление новой категории

**Шаг 1:** Добавьте категорию в `/data/categories.json`:

```json
{
  "id": "design",
  "name": "Дизайн",
  "image": "🎨",
  "promptCount": 3
}
```

**Шаг 2:** Создайте файл `/data/categories/design.json`:

```json
{
  "categoryId": "design",
  "prompts": [
    {
      "id": "design-1",
      "title": "Название промта",
      "description": "Описание"
    }
  ]
}
```

**Шаг 3:** Создайте файлы промтов в `/data/prompts/`.

## 🎨 Кастомизация

### Изменение цветов

Все цвета определены в CSS-переменных в файле `/public/css/styles.css`:

```css
:root {
    /* Фон */
    --bg-primary: #f5f5f7;
    --bg-secondary: #fbfbfd;
    
    /* Акценты */
    --accent-blue: #007AFF;
    --accent-purple: #5856D6;
    
    /* Текст */
    --text-primary: #1d1d1f;
    --text-secondary: #6e6e73;
}
```

### Изменение эмодзи категорий

Эмодзи категорий хранятся в поле `image` в файле `/data/categories.json`. Просто замените эмодзи на любой другой:

```json
{
  "id": "marketing",
  "name": "Маркетинг",
  "image": "🚀",  // Изменить здесь
  "promptCount": 5
}
```

## 🐛 Решение проблем

### Сервер не запускается

1. Убедитесь, что Node.js установлен: `node --version`
2. Убедитесь, что зависимости установлены: `npm install`
3. Проверьте, не занят ли порт 3000 другим приложением

### Промты не загружаются

1. Проверьте консоль браузера (F12) на ошибки
2. Проверьте, что все JSON файлы имеют правильный синтаксис
3. Проверьте, что ID в файлах совпадают

### Стили не применяются

1. Очистите кэш браузера (Ctrl+Shift+R)
2. Проверьте подключение CSS файла в index.html

## 📝 API Endpoints

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/categories` | Список всех категорий |
| GET | `/api/categories/:id` | Промты категории |
| GET | `/api/prompts/:id` | Полные данные промта |
| GET | `/api/search?q=query` | Поиск по промтам |

## 📄 Лицензия

MIT License — используйте свободно!

---

Создано с ❤️ для удобной работы с промптами


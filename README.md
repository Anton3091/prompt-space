# 🚀 PromtSpace - Portal для управления промтами

Современный веб-портал для хранения и управления промтами с дизайном в стиле **Apple Liquid Glass**.

![Version 1.0.0](https://img.shields.io/badge/version-1.0.0-blue)
![License MIT](https://img.shields.io/badge/license-MIT-green)

## ✨ Особенности

### Дизайн
- 🎨 **Liquid Glass Effect** - современный glassmorphism в стиле Apple
- 🌈 Светлая минималистичная тема с градиентными фонами
- 💎 Полупрозрачные элементы с размытием фона (backdrop-filter)
- ✨ Плавные анимации и микровзаимодействия
- 📱 Полностью адаптивный дизайн (375px - 1920px+)
- 🇷🇺 Весь интерфейс на русском языке

### Функциональность
- 📁 **Категории промтов** - организованное хранение по темам
- 🔍 **Живой поиск** - поиск по названиям, описаниям и тегам
- 📋 **Копирование в один клик** - быстрое копирование промтов
- 🎯 **Модальные окна** - удобный просмотр деталей промтов
- ⌨️ **Keyboard Navigation** - полная поддержка клавиатуры (ESC, Enter, Space)
- 🎭 **Loading States** - индикаторы загрузки данных

## 🛠 Технологический стек

### Frontend
- **React 19** - современная библиотека для UI
- **TypeScript** - типизация для надежности кода
- **Vite** - быстрая сборка и dev-сервер
- **Tailwind CSS** - utility-first стили через CDN
- **Lucide Icons** - красивая иконография

### Backend
- **Node.js + Express** - простой REST API сервер
- **JSON Files** - хранение данных без БД
- **ES Modules** - современный JavaScript

## 📁 Структура проекта

```
PromtSpace/
├── components/              # React компоненты
│   ├── GlassCard.tsx       # Универсальная карточка с glass эффектом
│   └── PromptModal.tsx     # Модальное окно для промтов
├── data/                    # JSON данные
│   ├── categories.json     # Список категорий
│   ├── categories/         # Промты по категориям
│   │   ├── cat-marketing.json
│   │   ├── cat-dev.json
│   │   └── cat-creative.json
│   └── prompts/            # Полные данные промтов
│       ├── p-mkt-1.json
│       ├── p-dev-1.json
│       └── ...
├── services/               # Сервисы для работы с данными
│   └── dataService.ts     # API для загрузки JSON
├── App.tsx                # Главный компонент приложения
├── index.html             # HTML точка входа
├── index.css              # Глобальные стили Liquid Glass
├── index.tsx              # React точка входа
├── server.js              # Express сервер
├── types.ts               # TypeScript типы
├── vite.config.ts         # Конфигурация Vite
└── package.json           # Зависимости проекта
```

## 🚀 Быстрый старт

### Предварительные требования
- Node.js >= 18.0.0
- npm >= 9.0.0

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd PromtSpace
```

2. **Установите зависимости**
```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Запуск production сервера

```bash
npm start
```

Это выполнит сборку и запустит Express сервер на `http://localhost:3000`

Или отдельными командами:
```bash
npm run build    # Сборка
npm run server   # Запуск сервера
```

## 📊 API Эндпоинты

### `GET /api/categories`
Получить список всех категорий

**Ответ:**
```json
[
  {
    "id": "cat-marketing",
    "name": "Маркетинг",
    "description": "Промты для маркетинга и рекламы",
    "iconName": "Megaphone",
    "gradient": "from-blue-500 to-cyan-500",
    "promptCount": 5
  }
]
```

### `GET /api/categories/:id`
Получить промты конкретной категории

**Пример:** `/api/categories/cat-marketing`

**Ответ:**
```json
{
  "categoryId": "cat-marketing",
  "prompts": [
    {
      "id": "p-mkt-1",
      "title": "Создание рекламного текста",
      "shortDescription": "Промт для генерации продающих текстов",
      "tags": ["реклама", "копирайтинг"]
    }
  ]
}
```

### `GET /api/prompts/:id`
Получить полную информацию о промте

**Пример:** `/api/prompts/p-mkt-1`

**Ответ:**
```json
{
  "id": "p-mkt-1",
  "categoryId": "cat-marketing",
  "title": "Создание рекламного текста",
  "shortDescription": "Краткое описание",
  "fullDescription": "Полное описание промта",
  "content": "Текст самого промта",
  "tags": ["реклама", "копирайтинг"]
}
```

### `GET /api/search?q=query`
Поиск по промтам

**Пример:** `/api/search?q=реклама`

**Ответ:** Массив промтов, соответствующих запросу

## 🎨 Дизайн-система

### Цветовая палитра

```css
/* Фон */
#f5f5f7        /* Основной фон */
#fbfbfd        /* Светлый вариант */

/* Стекло */
rgba(255, 255, 255, 0.7)   /* Базовая прозрачность */
rgba(255, 255, 255, 0.8)   /* Повышенная прозрачность */

/* Акценты */
#007AFF        /* Синий (Primary) */
#5856D6        /* Фиолетовый (Secondary) */

/* Текст */
#1d1d1f        /* Основной текст */
#6e6e73        /* Вторичный текст */
```

### Breakpoints

```css
375px   /* Mobile */
768px   /* Tablet */
1024px  /* Desktop */
1440px  /* Large Desktop */
1920px+ /* Extra Large */
```

### Эффекты Glass

```css
.glass-card {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 20px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
}
```

## 📝 Добавление нового промта

### 1. Создайте файл промта

`data/prompts/p-new-1.json`:
```json
{
  "id": "p-new-1",
  "categoryId": "cat-marketing",
  "title": "Название промта",
  "shortDescription": "Краткое описание для карточки (2-3 строки)",
  "fullDescription": "Подробное описание промта и области применения",
  "content": "Сам текст промта, который будет скопирован",
  "tags": ["тег1", "тег2", "тег3"]
}
```

### 2. Добавьте в категорию

`data/categories/cat-marketing.json`:
```json
{
  "categoryId": "cat-marketing",
  "prompts": [
    {
      "id": "p-new-1",
      "title": "Название промта",
      "shortDescription": "Краткое описание",
      "tags": ["тег1", "тег2"]
    }
  ]
}
```

### 3. Обновите счетчик

`data/categories.json`:
```json
{
  "id": "cat-marketing",
  "promptCount": 6  // Увеличьте на 1
}
```

## 🔧 Разработка

### Добавление новой категории

1. Обновите `data/categories.json`
2. Создайте `data/categories/cat-<id>.json`
3. Добавьте иконку из [Lucide Icons](https://lucide.dev)
4. Настройте градиент (Tailwind CSS)

### Кастомизация стилей

Основные стили находятся в:
- `index.css` - глобальные стили и утилиты
- `index.html` - конфигурация Tailwind
- Компоненты - inline Tailwind классы

### TypeScript типы

```typescript
// types.ts
export interface Category {
  id: string;
  name: string;
  description: string;
  iconName: string;
  gradient: string;
  promptCount: number;
}

export interface Prompt {
  id: string;
  categoryId: string;
  title: string;
  shortDescription: string;
  fullDescription?: string;
  content?: string;
  tags: string[];
}
```

## 🌐 Деплой

### Vercel
```bash
npm install -g vercel
vercel
```

### Netlify
```bash
npm run build
netlify deploy --prod --dir=dist
```

### Docker
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License - смотрите файл [LICENSE](LICENSE)

## 👨‍💻 Автор

Created with ❤️ for prompt management

## 🙏 Благодарности

- [React](https://react.dev/) - UI библиотека
- [Vite](https://vitejs.dev/) - Build tool
- [Tailwind CSS](https://tailwindcss.com/) - CSS framework
- [Lucide Icons](https://lucide.dev/) - Иконки
- [Express](https://expressjs.com/) - Backend framework

---

**PromtSpace** - Управляйте промтами красиво! ✨
